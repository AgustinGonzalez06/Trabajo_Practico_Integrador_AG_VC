<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <title>Registrar Administrador</title>
  <link rel="stylesheet" href="/css/stylesLogin.css" />
</head>
<body>
  <div class="login-cont">
    <h2>Registro de Administrador</h2>
<form action="/admin/registrar" method="POST">
<% if (typeof errores !== 'undefined' && errores.length > 0) { %>
  <ul class="errores">
    <% errores.forEach(e => { %>
      <li><%= e.msg %></li>
    <% }) %>
  </ul>
<% } %>
  <input type="email" name="email" placeholder="Correo electrónico" required value="<%= datos?.email || '' %>"/>
  <input type="password" name="password" placeholder="Contraseña" required />
  <button type="submit">Crear Administrador</button>
</form>
    <a href="/admin/login">Volver al login</a>
  </div>
  <div class="admin-login">
<a href="/loginUser.html" class="admin-btn">Ingresar como usuario</a>
</div>
</body>
</html>
